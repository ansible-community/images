[project]
name = "execution-environment-requirements"
version = "0.0.0"
description = "Generate locked Python dependencies for execution environments"
requires-python = ">=3.12"
dependencies = [
    "ansible-builder",
    "ansible-core",
    "PyYAML",
]

[dependency-groups]
dev = ["ruff"]

[tool.uv]
package = false
